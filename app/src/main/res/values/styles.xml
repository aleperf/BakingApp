<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CardViewStyle" parent="AppTheme">
        <item name="android:background">@color/cardColor</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/card_textview_color</item>
    </style>

    <style name="CardViewTitle" parent="CardViewStyle">
        <item name="android:textSize">21sp</item>
    </style>

    <!--Ingredient List in fragment_recipe_summary -->
    <style name="IngredientListTitle" parent="AppTheme">
        <item name="android:background">@color/ingredients_color_background</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">21sp</item>
        <item name="android:gravity">center</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textColor">@color/ingredients_color_text</item>
    </style>

    <style name="IngredientListItem" parent="AppTheme">
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/ingredients_color_text</item>
    </style>

    <style name="IngredientListQuantity" parent="AppTheme">
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">italic</item>
        <item name="android:textColor">@color/ingredients_color_text</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
